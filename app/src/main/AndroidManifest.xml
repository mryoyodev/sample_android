<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acer.projectfinaledit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".GoalActivity" />
        <activity android:name=".BirthdayActivity" />
        <activity android:name=".UsernameActivity" />
        <activity android:name=".ExlevelActivity" />
        <activity android:name=".HealthCareActivity" />
        <activity android:name=".DiseasesActivity" />
        <activity android:name=".FoodallergyActivity"></activity>
    </application>

</manifest>